<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /project/web
    <Directory /project/web>
        Options -Indexes
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
